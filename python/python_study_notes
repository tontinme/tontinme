#!/bin/bash

#chapter 2
	#变量，表达式和语句
	如果你不能确定变量或数据的类型，可以用解释器内置的函数type确认
		eg. >>type("Hello world")
			<type 'str'>
	#id，查看变量的唯一标识符
	  如果两个变量有相同的值，可能有两种情况：1.它们是两个不同的字符串，但有相同的值；2.它们指向同一个字符串，这个字符串有两个不同的名字。通过查看id可以区分这两种情况
	python区分变量名的大小写		
	python定义了28个关键字
	"and	continue	else	for	import	not	raise
	assert	def		except	from	in	or	return
	bread	del		exec	global	is	pass	try
	class	elif		finally	if	lambda	print	while"
#chapter 3 函数
	#函数的返回值	
	函数可以有多个返回值，但是只可能有一个return被执行，之后函数就终止了
	#数学函数模块
	python有一个math模块，提供了大部分与数学计算相似的函数
	#lambda函数
	lambda允许你定义一种单行的小函数，形式如下：
		lambda 参数: 表达式
#chapter 4 条件表达式
	#布尔表达式
	#逻辑表达式
	  and or not
	#条件语句
 	  if statement:
		pass	#pass表示什么都不做，是一条空语句
	  elif statement:
		statement
	  else statement:	
		statement
	#while循环
	#条件嵌套
	#return语句
	  return在函数中返回函数值
	#键盘输入
         python提供了内置的函数获得键盘的输入raw_input	
	 raw_input的参数是对要输入内容的提示
#chapter 5 字符串
	#操作符[]可以从字符串中取出任意个连续的字符,[]内的表达式是字符串的索引(索引从0开始)
	#[n:m]返回字符串的一部分,包括n,但不包括m
	  如果忽略n，则从0开始；如果忽略m，则到最后一个字符
	#可以用[]操作符修改字符串中的任何一个字符
	  eg. str = "python" newstr = "P" + str[1:]
	#函数len返回字符串的长度
	#用for遍历字符串
	  eg.for char in string:
	  	print char
	#字符串模块
	字符串模块string包含一些处理字符串的函数。import string
	string.lowercase包含所有小写的英文字母，string.uppercase-大写英文字母，string.digits-从0到9的数字
#chapter 6 列表
	#列表是一组任意类型的值，按照一定顺序组成的。列表中的元素(索引从0开始)可以是任意类型，甚至是列表类型（嵌套）
	#列表中的元素用中括号括起来，以逗号分隔
	#列表中的元素可以是变量，但是修改变量的值，并不影响列表中元素的值
	#range
	  range返回一个整数列表。range(n,m)-返回n,n+1,...,m-1(m>n)
	  单一参数的range返回从0开始的列表
	  3个参数的列表，返回成等差数列的列表
	#不包含元素的列表，称为空列表
	#列表可以赋值，或者作为参数传递给函数
	#列表的索引可以是任何整数表达式
	  索引也可以是一个负数，那么列表的最后一个元素的索引是-1，倒数第二个的索引是-2，以此类推
	#列表的一些方法
	  append	在列表的尾部追加元素
	  insert	在列表中插入元素。insert(索引位置，插入元素的值)
	  extend	合并两个列表为一个
	  index		取得元素的索引值
	  remove	从列表中删除第一次出现的值
	  pop		删除最后一个值,然后返回这个值
	#len返回列表的长度，即元素的个数
	  虽然列表之中还可以包含列表，但嵌套的列表被当作一个元素
	#可以用for循环遍历列表
	  任何列表表达式都可以用于for循环中
	#加号操作符+将两个列表连接在一起
	  类似的，乘号操作符"*"表示把列表所有元素重复一定的次数，然后形成一个新的列表
	#通过操作符"[]"，将列表元素连续取出的部分，叫片段
	#和字符串不一样，列表元素是可更改的
	  可以通过给元素赋空值，删除一些元素
	  可以利用列表片段，在特定的位置插入元素	eg.list[4:4] = ['example']
	  关键字del删除列表中的元素	eg. >>del list[1]	>>del list[2:5]
	#列表在内存中的位置
	  和字符串的一个区别,即使列表变量的值相同，它们也是指向不同的列表值
	  别名.当然，如果我们将一个变量赋值给另一个变量，那么两个变量指向同一个对象
	  克隆列表.列表的克隆是产生两个值一样，但却有不同标识符的列表
	    克隆的方法是利用片段操作	y = x[:]
	#嵌套列表可以代表矩阵
	#string.split可以将字符串分隔成单词列表
	#列表映射
	可以对列表中的所有元素做相同的操作
	  也可以加上if判断对部分元素做操作
#chapter 7 序列
	#序列(tuple)和列表相近，只是它的元素的值是固定的。
	  序列中的元素以逗号分割，序列用小括号括起来
	  如果要创造一个包含一个元素的序列，需要在序列的最后加上逗号(不加逗号会被当成字符串)
	  也可以用片段操作符获取序列的一部分
	#序列赋值
	  python中交换变量的值
	  >>>a=1,b=2,c=3
	  >>>print a,b,c
	     1,2,3
	  >>>a,b,c=c,b,a
	  >>>print a,b,c
	     3,2,1
	  右侧若含有表达式，先进行计算，再赋值
	#python别名问题
	  在python中如果变量具有相同的值，就可以认为这些变量是同一个值的别名，也就是这些变量指向同一个值。如果\
		  某一个变量改变了它自己的值，那么解释器就开辟新的内存空间存储这个变量的值，这个变量就指向新的值
	#函数的返回值可以是序列
	#随机函数
	  python提供了内建的函数来生成伪随机数(不是数学意义上的随机数).random模块中的random函数能产生一个值的\
		  范围在0.0到1.0之间的浮点数
#chapter 8 字典
	#字典的索引可以是字符串(也可以是整数)
	  之前的三种数据类型：字符串，列表和序列，都使用整数做索引
	#字典的元素以逗号为分隔符，每个元素包含键和键值，它俩用冒号分开
	  字典的键值顺序与创建时的顺序不同,我们通过键浏览对应的值
	#字典操作
	  删除元素
	  	>>>del dict_name['index1']
	  改变元素的值
#chapter 10 异常
	  	>>>dict_name['index2']='new_value'
	  删除所有元素
	  	>>>dict_name.clear()
	  函数len返回字典元素的数量
	  	>>>len(dict_name)
	#别名和拷贝
	  别名
	  	dict_new = dict_original
	  拷贝
	  	dict_copy = dict_original
		别名和源字典指向同一个值，拷贝则指向全新的拷贝(即更改dict_copy，不影响dict_original)
	#稀疏矩阵
	  字典可以用来表示矩阵
	  矩阵的非零元素的键用含有两个整数元素的序列表示，分别代表行和列
		eg.matix = {(0,3): 1, (2,1): 2}
	  矩阵中没有存储值为0的元素，可以用get方法解决该问题
	  	>>>matix.get((0,2),0)	#第一个参数是键，第二个参数表示如果该键没有出现在字典中，那么这个键的\
			值就是第二个参数
	#暗示
	#计算字符串
	  字典提供了一个很好的办法，统计字母出现的次数
	  字典提供items和sort函数，分别提供'列出元素'和'对键进行排序'的功能
#chapter 9 文件
	#文件的打开和关闭
	  打开文件就是创造了文件对象
	  	>>>f = open("test.dat", w)	#以写入的方式打开文件，如果文件不存在，就创建它；否则，新建的\
			文件覆盖原有的
	  将文件写到字符串，使用read方法
	  	text = f.read()		#read()可以接受数字参数，表示读取一定数量的字符.如果读到文件末尾，就\
			返回空字符
	#文本文件
	  文本文件包含可打印字符和空格，每一行以换行符为结束标志
	  readline方法每次从文本文件中读取一行的内容，包括换行符
	  readlines方法以列表的方法返回文件内其余的内容
	  若已到达文件末尾，调用readline()返回空字符，调用readlines返回空列表
	#写入变量
	  write的参数只能是字符串，若想把其他类型的变量写入文件，就必须将其转换成字符串
	  可以使用str函数
	  	>>>f.write(str(1000))
	  也可以利用格式化操作符"%"
	  	当操作符%的两边都是整数时，它是求余数的运算；
		如果第一个操作符是字符串，它就是格式化操作符；第一个参数是需要格式化的字符串，第二个参数是数组\
			表达式
	  %d-整数，%f-浮点数(默认小数点后面6位)，%s-字符串，%5d-占5位的整数（若位数不够，在前面加空格（-5在\
		  在后面加空格））
	  %4.3f-指定位数的浮点数，%-6s - 长度为6的字符串，长度不够6的在后面补空格
	#pickle模块
	  它提供的方法把各种类型的数据存入文件，数据结构的信息也被保存了(即不会把存储的内容都转换成字符串)
#chapter 10 异常
	#错误信息
	  异常信息分为两个部分：冒号前面的是异常类型，之后是对此的简单说明
	  当异常产生时，如果没有代码来处理它，python对其进行缺省处理，输出一些异常信息并终止程序
	  程序产生异常可以用try和except语句来捕获处理
	#自定义异常信息
	  如果程序检测到错误，我们也可以用raise定义异常
	  raise有两个参数，第一个是由我们自己定义的异常类型，第二个是关于此异常的少量说明信息
	  如果在try,except语句中使用了except:(即对异常的默认处理)，很有可能把应该注意的程序错误隐藏了，为防止\
		  这种情况，可以使用raise，将异常抛出。
	  	>>>try:
			...
	  	>>>except:
		>>>	print "Unexpected Error!"
		>>>	raise
		>>>else:
		>>>	print "There is no error!"
	  如果没有任何异常发生，上面的else语句的内容将会执行.else语句一定放在所有except语句的后面
#chapter 11 类和对象
