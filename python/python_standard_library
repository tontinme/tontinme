#chapter 2 
	#exception模块
	该模块定义了以下标准异常：
	exception	所有异常的基类，强烈建议（但不是必须）自定义的异常也继承这个类
	SystemExit	由sys.exit函数引发。如果它在最顶层没有被try-except语句捕获，那么解释器将直接关闭而不会显示任何跟踪返回信息
	StandardError	是所有内建异常的基类（除SystemExit外）
	KeyboardInterrupt
	在用户按下Ctrl+C（或其他打断按键）后被引发。如果它在你使用“捕获所有”的try-except语句时可能会导致奇怪的问题
	ImportError	在python导入模块失败时被引发
	environmentError	
	作为所有解释器环境引发异常的基类（也就是说，这些异常一般不是由程序bug引起的）
	IOError		用于标记I/O相关错误
	OSError		用于标记os模块引起的错误
	WindowsError	用于标记os模块中的windows相关错误
	NameError	在Python查找全局或局部名称失败时被引发
	UnboundLocalError
	当一个局部变量还没有赋值就被引用时，会引发该异常
	AttributeErro	当python寻找（或赋值）给一个实例属性，方法，模块功能或其他有效的命名失败时，会引发这个异常
	SystaxError	当解释器在编译时遇到语法错误，这个异常会被引发



